<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MVPParteSofi.MVVM.View.HistorialFrecuencia"
             Title="HistorialFrecuencia"
             NavigationPage.HasNavigationBar="False">
    
    <!--Proximamente sólo en cines...-->
    
    <Grid RowDefinitions=".7*,.5*"
          Margin="10">
            <Grid
                Grid.Row ="1"
                RowDefinitions=".1*,.9*">
                <VerticalStackLayout
                    Grid.Row="0">
                    <Label Text="Filtrar resultados"
                           Margin="10,0,0,0"></Label>
                <HorizontalStackLayout
                        HorizontalOptions="CenterAndExpand">
                    <DatePicker FontSize="17"
                                Margin="10"
                                BackgroundColor="White">
                    </DatePicker>
                    <TimePicker FontSize="17"
                                Margin="10">

                    </TimePicker>
                </HorizontalStackLayout>

                </VerticalStackLayout>
                <CollectionView Grid.Row="1"
                                EmptyView="No se encontraron datos"></CollectionView>
        </Grid>
    </Grid>

</ContentPage>